# OpenClaw 生态系统深度研究报告

## 概述

**OpenClaw** 是一个本地优先的 AI 智能体（Agent）运行时环境，类似于 "tmux: confusing at first, then suddenly you can't live without me"。它提供了一个完整的框架，让用户可以在本地设备上运行 AI 代理，连接多种通信渠道（Telegram、WhatsApp、Discord 等），并通过可扩展的 Skill 系统与外部工具和服务交互。

---

## 一、核心架构

### 1.1 系统架构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              OpenClaw 生态系统                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │   Channels   │  │    Agent     │  │    Skills    │  │   Memory     │   │
│  │  (通信渠道)   │  │   (智能体)    │  │   (技能)     │  │   (记忆)      │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│         │                 │                 │                 │          │
│         └─────────────────┴─────────────────┴─────────────────┘          │
│                                     │                                      │
│                           ┌─────────┴─────────┐                           │
│                           │  Gateway (网关)    │                           │
│                           │  WebSocket 服务    │                           │
│                           └─────────┬─────────┘                           │
│                                     │                                      │
│  ┌──────────────────────────────────┼──────────────────────────────────┐  │
│  │                        Tools Layer (工具层)                        │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ │  │
│  │  │Browser │ │  Cron  │ │ Message│ │  Exec  │ │ Canvas │ │  Node  │ │  │
│  │  │(浏览器)│ │(定时器)│ │(消息)  │ │(执行)  │ │(画布)  │ │(节点)  │ │  │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心组件

| 组件 | 功能 | 关键特性 |
|------|------|----------|
| **Gateway** | WebSocket 网关服务 | 消息路由、会话管理、工具调度 |
| **Agent** | AI 智能体运行时 | 多模型支持（Claude、Gemini、Kimi 等）|
| **Channels** | 通信渠道集成 | Telegram、WhatsApp、Discord、Slack、iMessage 等 |
| **Skills** | 可扩展技能系统 | 57+ 内置技能，支持自定义开发 |
| **Memory** | 持久化记忆系统 | 日度/长期记忆、语义搜索 |
| **Cron** | 定时任务调度 | 精确时间触发、隔离会话执行 |
| **Browser** | 浏览器自动化 | Chrome 控制、Deep Research 支持 |
| **Canvas** | 可视化渲染 | HTML/JS 渲染、截图 |

---

## 二、目录结构

### 2.1 全局配置目录 (`~/.openclaw/`)

```
~/.openclaw/
├── agents/              # 智能体配置
│   └── main/            # 主智能体
│       ├── agent/
│       ├── credentials/
│       └── workspace/   # 工作空间
├── browser/             # 浏览器配置
├── canvas/              # Canvas 配置
├── completions/         # 补全配置
├── credentials/         # 凭证存储
├── cron/                # 定时任务配置
├── devices/             # 设备配对信息
├── identity/            # 身份配置
├── logs/                # 日志文件
├── skills/              # 全局技能
├── subagents/           # 子智能体
├── telegram/            # Telegram 配置
├── openclaw.json        # 主配置文件
└── workspace/           # 默认工作空间
```

### 2.2 工作空间结构 (`workspace/`)

```
workspace/
├── AGENTS.md           # 智能体规则
├── SOUL.md             # 智能体人格
├── USER.md             # 用户信息
├── MEMORY.md           # 长期记忆
├── HEARTBEAT.md        # 定时任务清单
├── TOOLS.md            # 本地工具配置
├── docs/               # 文档
├── memory/             # 每日记忆
│   └── YYYY-MM-DD.md
├── skills/             # 自定义技能
├── investment/         # 投资相关
│   └── reports/        # 研究报告
├── scripts/            # 脚本
└── notes/              # 笔记
```

---

## 三、技能系统 (Skills)

### 3.1 技能概述

Skills 是 OpenClaw 的扩展机制，每个 Skill 是一个封装好的功能模块，包含：
- **SKILL.md**: 使用文档
- **脚本/工具**: 实际执行的工具
- **配置**: 依赖和环境设置

### 3.2 技能来源

| 来源 | 位置 | 数量 | 说明 |
|------|------|------|------|
| **openclaw-bundled** | `/opt/homebrew/lib/node_modules/openclaw/skills/` | 40+ | 官方内置技能 |
| **openclaw-workspace** | `~/.openclaw/workspace/skills/` | 10+ | 用户自定义技能 |
| **openclaw-managed** | `~/.openclaw/skills/` | 1+ | 管理类技能 |

### 3.3 核心技能分类

#### 📱 通信与消息
| 技能 | 功能 | 状态 |
|------|------|------|
| wacli | WhatsApp 消息发送/搜索 | ✅ ready |
| imsg | iMessage/SMS 管理 | ✅ ready |
| himalaya | IMAP/SMTP 邮件管理 | ✅ ready |
| gog | Google Workspace (Gmail, Calendar, Drive) | ✅ ready |

#### 🌐 网络与浏览器
| 技能 | 功能 | 状态 |
|------|------|------|
| browser | Chrome 浏览器自动化 | ✅ ready |
| web_search | Brave 搜索 API | ✅ ready |
| web_fetch | 网页内容提取 | ✅ ready |
| summarize | URL/播客/文件摘要 | ✅ ready |

#### 💻 开发与工具
| 技能 | 功能 | 状态 |
|------|------|------|
| coding-agent | Codex/Claude Code/OpenCode 控制 | ✅ ready |
| github | GitHub CLI 集成 | ✅ ready |
| mcporter | MCP 服务器管理 | ✅ ready |
| oracle | Prompt + 文件打包工具 | ✅ ready |

#### 📝 笔记与知识管理
| 技能 | 功能 | 状态 |
|------|------|------|
| obsidian | Obsidian vault 管理 | ✅ ready |
| bear-notes | Bear 笔记管理 | ✅ ready |
| apple-notes | Apple Notes 管理 | ✅ ready |
| notion | Notion API 集成 | ✅ ready |

#### 🎵 媒体与娱乐
| 技能 | 功能 | 状态 |
|------|------|------|
| sonoscli | Sonos 音箱控制 | ✅ ready |
| blucli | BluOS 控制 | ✅ ready |
| openhue | Philips Hue 控制 | ✅ ready |
| songsee | 音频可视化 | ✅ ready |
| video-frames | 视频帧提取 | ✅ ready |

#### 💰 投资与金融 (Friday 自定义)
| 技能 | 功能 | 状态 |
|------|------|------|
| gemini-deep-research | Gemini 深度研究自动化 | ✅ ready |
| dcf-valuation | DCF 估值模型 | ✅ ready |
| comparative-valuation | 相对估值分析 | ✅ ready |
| stock-quote | 股票实时行情 | ✅ ready |
| portfolio-risk | 投资组合风险分析 | ✅ ready |
| technical-analysis | 技术分析指标 | ✅ ready |
| friday-report-sync | 研究报告同步 | ✅ ready |

### 3.4 技能管理命令

```bash
# 查看已安装技能
openclaw skills list

# 从 ClawHub 搜索技能
npx clawhub search <keyword>

# 安装技能
npx clawhub install <skill-name>

# 同步技能到最新版本
npx clawhub sync
```

---

## 四、定时任务系统 (Cron)

### 4.1 架构特点

OpenClaw 的 Cron 系统具有独特的设计：

| 特性 | 说明 |
|------|------|
| **隔离会话** | 每个任务在独立会话中运行，避免上下文污染 |
| **精确时间** | 基于系统时间精确触发 |
| **自动通知** | 任务完成自动通知主会话 |
| **失败重试** | 支持失败后的重试机制 |

### 4.2 任务类型

```yaml
# systemEvent - 向主会话注入消息
payload:
  kind: "systemEvent"
  text: "提醒: NVIDIA财报今晚发布"

# agentTurn - 在隔离会话中运行智能体
payload:
  kind: "agentTurn"
  message: "执行戴维斯双击扫描..."
  model: "kimi-coding/k2p5"
```

### 4.3 Friday 的定时任务配置

| 时间 | 任务 | 模型 | 同步报告 |
|------|------|------|----------|
| 每 6 小时 | QMD 索引更新 | 系统命令 | - |
| 00:00-02:00 | 每日持续学习 | kimi-coding/k2p5 | - |
| 周二/四 05:00 | 戴维斯双击扫描 | main agent | ✅ |
| 每天 06:00 | 股市综合晨报 | main agent | ✅ |
| 每天 08:00 | Portfolio 交易决策 | kimi-coding/k2p5 | - |
| 周六 13:00 | 投资逻辑分析 | main agent | ✅ |
| 周六 14:30 | 美股主线分析 | main agent | ✅ |

### 4.4 Cron 管理命令

```bash
# 查看定时任务
openclaw cron list

# 添加任务
openclaw cron add --job '<job-json>'

# 立即运行任务
openclaw cron run --jobId <id>

# 查看任务执行历史
openclaw cron runs --jobId <id>
```

---

## 五、浏览器自动化 (Browser)

### 5.1 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    Browser 自动化架构                     │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │ OpenClaw    │───▶│  Chrome     │───▶│   Web       │ │
│  │ Extension   │    │  Browser    │    │   Pages     │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
│         │                  │                 │         │
│         │    WebSocket     │    CDP          │         │
│         └──────────────────┘                 │         │
│                                              │         │
│  ┌───────────────────────────────────────────┘         │
│  │  Browser Control Server (端口 9222)                  │
│  │  - 标签页管理                                         │
│  │  - 元素操作 (click, type, scroll)                    │
│  │  - 快照捕获 (snapshot)                               │
│  │  - 截图 (screenshot)                                 │
│  └─────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────┘
```

### 5.2 核心功能

| 功能 | 命令 | 说明 |
|------|------|------|
| **标签页管理** | `browser tabs` | 列出所有标签页 |
| **页面导航** | `browser navigate` | 导航到指定 URL |
| **元素操作** | `browser act` | click, type, press, scroll |
| **页面快照** | `browser snapshot` | 获取页面结构和内容 |
| **截图** | `browser screenshot` | 页面截图 |
| **PDF 导出** | `browser pdf` | 保存为 PDF |

### 5.3 Gemini Deep Research 工作流

```bash
# 1. 检查浏览器状态
browser status

# 2. 获取已连接标签页
browser tabs

# 3. 聚焦到目标标签页
browser focus targetId=<targetId>

# 4. 导航到 Gemini
browser navigate targetId=<targetId> url="https://gemini.google.com/app"

# 5. 启用 Deep Research 模式
browser act targetId=<targetId> request='{"kind": "click", "ref": "<tools-button>"}'

# 6. 输入研究提示词
browser act targetId=<targetId> request='{"kind": "type", "ref": "<input>", "text": "..."}'

# 7. 发送请求
browser act targetId=<targetId> request='{"kind": "press", "key": "Enter"}'

# 8. 轮询等待结果 (每 10-15 秒检查一次)
browser snapshot targetId=<targetId> refs=aria
```

### 5.4 浏览器守护程序

```bash
# 启动守护程序
./scripts/browser_daemon.sh start

# 查看状态
./scripts/browser_daemon.sh status

# 停止守护程序
./scripts/browser_daemon.sh stop

# 添加任务到队列
./scripts/browser_daemon.sh queue ./scripts/run_deep_research.sh
```

---

## 六、记忆系统 (Memory)

### 6.1 双层记忆架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Memory 系统                             │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │  短期记忆 (Session Context)                          │   │
│  │  - 当前对话上下文                                     │   │
│  │  - 工具调用历史                                       │   │
│  │  - 临时变量                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  日度记忆 (Daily Memory)                             │   │
│  │  memory/YYYY-MM-DD.md                               │   │
│  │  - 每日原始日志                                       │   │
│  │  - 任务执行记录                                       │   │
│  │  - 学习笔记                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  长期记忆 (Long-term Memory)                         │   │
│  │  MEMORY.md                                          │   │
│  │  - 提炼的智慧                                        │   │
│  │  - 投资框架                                          │   │
│  │  - 重要决策记录                                       │   │
│  │  - 用户偏好                                          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 知识库搜索 (QMD)

**QMD** (Query Markdown Database) 是 OpenClaw 的语义搜索系统：

```bash
# BM25 全文搜索
qmd search "投资框架"

# 向量语义搜索
qmd vsearch "股票分析策略"

# 混合搜索（推荐）
qmd query "戴维斯双击"

# 获取特定片段
qmd get MEMORY.md:50 -l 20

# 更新索引
qmd update
```

**强制规则**: 读取大文件前必须先 QMD 搜索，节约 Token。

### 6.3 记忆管理最佳实践

1. **每日维护**: 定期回顾 daily memory，提炼到 MEMORY.md
2. **去除过时**: 删除不再相关的记忆
3. **结构清晰**: 使用标题、表格、列表组织信息
4. **关键决策**: 重要决策必须记录到 MEMORY.md

---

## 七、CLI 命令体系

### 7.1 核心命令

```
openclaw [options] [command]

Options:
  --dev             # 开发模式（隔离状态）
  --profile <name>  # 使用指定配置文件
  -V, --version     # 版本信息

Commands:
  # 网关管理
  gateway           # Gateway 控制
  daemon            # 守护进程（legacy）
  health            # 健康检查
  doctor            # 诊断修复

  # 智能体管理
  agent             # 运行智能体
  agents            # 管理隔离智能体
  sessions          # 会话管理

  # 通信渠道
  channels          # 渠道管理
  message           # 发送消息

  # 定时任务
  cron              # Cron 调度器

  # 浏览器
  browser           # 浏览器控制

  # 技能
  skills            # 技能管理

  # 配置
  config            # 配置管理
  configure         # 交互式配置
  setup             # 初始化

  # 其他
  status            # 状态查看
  update            # 更新
  logs              # 日志
```

### 7.2 常用命令示例

```bash
# 检查系统状态
openclaw status

# 启动 Gateway
openclaw gateway start

# 发送 Telegram 消息
openclaw message send --channel telegram --target @chat --message "Hello"

# 运行智能体
openclaw agent --to +1234567890 --message "分析今天股市"

# 查看定时任务
openclaw cron list

# 浏览器操作
openclaw browser tabs
openclaw browser snapshot
```

---

## 八、通信渠道 (Channels)

### 8.1 支持的渠道

| 渠道 | 状态 | 说明 |
|------|------|------|
| **Telegram** | ✅ 已配置 | Bot API，支持按钮、回复 |
| **WhatsApp** | ✅ 可用 | WhatsApp Web，需扫码登录 |
| **Discord** | ✅ 可用 | Bot/Webhook |
| **Slack** | ✅ 可用 | App/Bot |
| **iMessage** | ✅ 可用 | macOS 原生 |
| **Signal** | ✅ 可用 |  Signal CLI |
| **Google Chat** | ✅ 可用 | Workspace |
| **Email** | ✅ 可用 | IMAP/SMTP |

### 8.2 消息路由

```
用户消息 ──▶ Channel ──▶ Gateway ──▶ Agent Session ──▶ Tool Call
                                      │
                                      ▼
                              ┌───────────────┐
                              │  Skill Router │
                              └───────┬───────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
               ┌────────┐      ┌──────────┐      ┌──────────┐
               │ Browser│      │  Exec    │      │ Message  │
               └────────┘      └──────────┘      └──────────┘
```

---

## 九、安全与隐私

### 9.1 安全设计原则

| 原则 | 实现 |
|------|------|
| **本地优先** | 所有数据存储在本地，不上传云端 |
| **凭证加密** | API Keys、Tokens 加密存储 |
| **执行审批** | 敏感操作需用户确认 |
| **沙箱隔离** | 子智能体在隔离环境中运行 |

### 9.2 凭证管理

```
~/.openclaw/credentials/
├── auth-profiles.json      # 认证配置
├── channel-tokens.json     # 渠道令牌
└── exec-approvals.json     # 执行审批记录
```

### 9.3 信任评分系统

Friday 实现了 **Trust Score (信誉度)** 机制：

| 分数 | 等级 | 状态 |
|------|------|------|
| 90-100 | 🟢 卓越 | 完全信任 |
| 70-89 | 🟡 良好 | 正常操作 |
| 50-69 | 🟠 警戒 | 受限确认 |
| <50 | 🔴 失信 | 暂停操作 |

**减分项**: 编造数据(-30~-50), 使用估算值冒充实时, 产生幻觉数据

---

## 十、Friday 的定制化扩展

### 10.1 投资智能体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Friday Investment Intelligence           │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ Data Layer  │  │ Analysis    │  │  Decision Engine    │ │
│  │ - LongPort  │  │ - Gemini    │  │  - Portfolio Risk   │ │
│  │ - Yahoo     │  │ - Opus      │  │  - Technical Signal │ │
│  │ - CMC       │  │ - Kimi      │  │  - Valuation Model  │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│           │              │                  │              │
│           └──────────────┴──────────────────┘              │
│                          │                                 │
│              ┌───────────┴───────────┐                     │
│              │   Report Generator    │                     │
│              │   - Markdown          │                     │
│              │   - HTML              │                     │
│              │   - Sync to DB        │                     │
│              └───────────┬───────────┘                     │
│                          │                                 │
│              ┌───────────┴───────────┐                     │
│              │   Friday Web Portal   │                     │
│              │   danielzhuang.xyz    │                     │
│              └───────────────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 自定义技能

Friday 开发了 8 个投资专用技能：

1. **gemini-deep-research**: Gemini 深度研究自动化
2. **friday-report-sync**: 研究报告同步到数据库
3. **dcf-valuation**: DCF 估值模型
4. **comparative-valuation**: 相对估值分析
5. **portfolio-risk**: 投资组合风险分析
6. **technical-analysis**: 技术分析指标
7. **stock-quote**: 股票实时行情
8. **notebooklm-youtube-sync**: YouTube 视频同步到 NotebookLM

### 10.3 自动化工作流

```mermaid
 Cron 定时触发
      │
      ▼
┌─────────────┐
│ Deep Research│
│ (Gemini)     │
└──────┬───────┘
       │
       ▼
┌─────────────┐
│ Report Gen   │
│ (Markdown)   │
└──────┬───────┘
       │
       ▼
┌─────────────┐
│ Sync to DB   │
│ (SQLite)     │
└──────┬───────┘
       │
       ▼
┌─────────────┐
│ Notify       │
│ (Telegram)   │
└─────────────┘
```

---

## 十一、部署与运维

### 11.1 系统要求

| 组件 | 要求 |
|------|------|
| **OS** | macOS (主要支持), Linux |
| **Node.js** | v18+ |
| **Chrome** | 最新版 |
| **内存** | 4GB+ (推荐 8GB) |
| **存储** | 1GB+ |

### 11.2 安装方式

```bash
# npm 安装
npm install -g openclaw

# 初始化
openclaw setup

# 交互式配置
openclaw configure

# 启动 Gateway
openclaw gateway start
```

### 11.3 生产环境部署

```bash
# 生产服务器配置
Server: 43.134.37.253 (腾讯云)
Domain: https://danielzhuang.xyz/
Stack: Flask + Gunicorn + SQLite
Port: 5001

# 关键脚本
./deploy.sh          # 部署
./manage.sh          # 管理
./restart-dev.sh     # 重启服务
./monitor-dev.sh     # 监控
```

### 11.4 监控与日志

```bash
# 查看服务状态
ps aux | grep gunicorn

# 查看日志
tail -f logs/app.log
tail -f server.log

# 数据库备份
sqlite3 friday.db ".backup backup/friday_$(date +%Y%m%d).db"

# Gateway 健康检查
openclaw health
```

---

## 十二、生态系统对比

| 特性 | OpenClaw | AutoGPT | LangChain | Custom GPTs |
|------|----------|---------|-----------|-------------|
| **本地优先** | ✅ 完全本地 | ⚠️ 混合 | ⚠️ 库/SDK | ❌ 云端 |
| **多渠道路由** | ✅ 内置 | ❌ 需自建 | ❌ 需自建 | ❌ 有限 |
| **Skill 系统** | ✅ 成熟 | ⚠️ 插件 | ✅ 工具 | ⚠️ Actions |
| **浏览器自动化** | ✅ 原生 | ❌ | ❌ | ❌ |
| **定时任务** | ✅ 内置 | ❌ | ❌ | ❌ |
| **代码执行** | ✅ 安全沙箱 | ⚠️ 有风险 | ⚠️ 需配置 | ❌ |
| **记忆系统** | ✅ 双层 | ⚠️ 向量DB | ⚠️ 需集成 | ⚠️ 有限 |
| **隐私控制** | ✅ 完全控制 | ⚠️ 依赖配置 | ⚠️ 依赖配置 | ❌ 上传云端 |

---

## 十三、未来发展展望

### 13.1 短期优化方向

1. **Skill 生态扩展**: 更多垂直领域技能
2. **性能优化**: 更快的 Gateway 响应
3. **移动端支持**: iOS/Android App
4. **多模态增强**: 图像、音频、视频处理

### 13.2 长期愿景

1. **自主智能体**: 长期运行的自主任务执行
2. **多智能体协作**: 智能体之间的协作与通信
3. **知识图谱**: 结构化的知识表示与推理
4. **个性化学习**: 根据用户行为持续优化

---

## 十四、总结

OpenClaw 是一个**功能完备、架构清晰、扩展性强**的本地 AI 智能体平台。其核心优势在于：

1. **本地优先**: 数据隐私完全可控
2. **多渠道**: 统一接口对接多种通信方式
3. **Skill 系统**: 强大的可扩展性
4. **浏览器自动化**: 独特的 Deep Research 能力
5. **定时任务**: 精确可靠的自动化调度
6. **记忆系统**: 双层记忆 + 语义搜索

对于需要**隐私保护、高度定制、自动化工作流**的用户，OpenClaw 是目前最优的选择之一。

---

**报告版本**: 1.0  
**生成时间**: 2026-02-17  
**作者**: Friday  
**文档来源**: OpenClaw CLI v2026.2.6-3, Workspace 配置, Skill 文档
